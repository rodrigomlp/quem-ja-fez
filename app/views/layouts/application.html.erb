<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Quem Já Fez?</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <!-- Google fonts -->
    <!-- <link href="https://fonts.googleapis.com/css?family=Alegreya+Sans:300,400|Pacifico" rel="stylesheet"> -->
    <link href="https://fonts.googleapis.com/css?family=Didact+Gothic|Pacifico|Titillium+Web:300,400" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/css/select2.min.css" rel="stylesheet" />

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <!-- Google fonts -->



  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <%= render 'shared/navbar/navbar' %>
    <%= render 'shared/flashes' %>
    <%= yield %>
    <div id='remote_container'></div>
    <!-- Include Required Prerequisites -->
    <script>
      <% if params[:user_id] %>
        window.user_undergraduate = <%= current_user && current_user.undergraduate %>;
        window.resume_id = <%= params[:user_id] %>
        window.is_same_user = <%= current_user == Resume.find(params[:user_id]).user %>
      <% end %>
    </script>


    <%= render 'shared/footer' %>
    <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-105676747-1', 'auto');
        ga('send', 'pageview');
    </script>
    <%= javascript_include_tag 'application' %>
    <%= yield(:js) %> <!-- Reload javascript that was called from within an html -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/js/select2.min.js"></script>


  </body>
</html>
